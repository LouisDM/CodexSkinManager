# Changelog

## 1.1.0 — 未发布

### Codex 主题显示

- 修复三套皮肤的主题名与 Codex 左下角用户名重叠的问题。根因是主题名使用侧栏 `::after`，作为最后一个 Flex 项落入了原生账户栏占用的底部区域。
- 玄刃夜行、红莲业火和不死凰焰现在统一把装饰标题与主题名排在所有原生侧栏内容之前，主题名显示在左上身份区，不再占用用户名、任务列表或主内容区。
- 新增三模板真实浏览器布局契约测试，要求装饰标题、主题名、原生侧栏内容保持 `-2 / -1 / 0` 的安全顺序。

### 皮肤资料库

- 修复同一皮肤的 `1.0.0` 与 `1.0.1` 被显示成两张卡片的问题；资料库现在按稳定皮肤 ID 合并版本，只展示最高语义版本。
- 历史版本仍保留在管理器存储中，避免破坏旧的应用记录；“最近使用”按皮肤身份关联，因此升级后仍能找到对应的新版本卡片。
- 修复宽幅预览图和较长安装路径把详情内容撑出可视区域的问题；详情内容现在严格跟随可用宽度，预览图固定为 16:10，状态卡片和元数据会按栏宽自适应排版。
- 右侧详情改为可独立收起、展开和拖动调整宽度的面板；工具栏按钮和 `⌥⌘I` 均可切换，资料库可在不查看详情时使用完整窗口宽度。
- 将“筛选侧栏 + 大卡片列表 + 详情”的重复三栏改为真正的双栏主从工作区：左侧紧凑列表直接承担皮肤切换，右侧成为唯一的完整预览与操作区。
- 四种资料库筛选收进顶部菜单；导航行会直接显示缩略图、名称、作者、版本、授权和使用状态，筛选结果同时显示可见数量与总数。
- 收起详情时，同一资料库自动切换为响应式大卡片画廊；重新展开后恢复紧凑导航，不会留下空白或重复大预览。
- 将导入与导出并排收进窗口顶部的“皮肤文件”操作组，移除左下角孤立导入按钮和详情“更多”中的隐藏导出入口；“文件”菜单同步提供 `⌘O` 导入与 `⇧⌘E` 导出。
- 可分享皮肤可直接导出为确定性 `.codexskin`；仅限本机皮肤的导出按钮显示锁定状态与授权原因。
- 修复导出包回导同一资料库时因图片净化改变 manifest 文件描述符而被误判为“同版本内容冲突”的问题；现在比较实际净化后文件与稳定元数据，真实内容不同仍会拒绝。

### 皮肤制作流程

- 新增通用 `build_codexskin.py`，从一份 `skin.json + assets + LICENSES` 源目录直接生成 `.codexskin`，不再要求先制作 `.command`。
- 打包器严格校验字段、模板、令牌、素材路径、图片文件头、许可和权利声明，并自动生成 manifest、字节数与 SHA-256。
- 输出为确定性 data-only store ZIP；未引用文件、CSS、JavaScript、Shell 和其他主动内容不会进入包。
- 新增 Python 打包契约测试和 Swift 导入器跨语言兼容测试。
- 新增 `.codexskin` 作者规范，说明现有模板、新模板扩展和旧启动器迁移边界。

### 本机私有皮肤迁移

- 新增管理器签名模板 `undying-phoenix-v1`，用于“柳七月 · 不死凰焰”。
- 新增 `build_private_liu_qiyue_skin.py`，可把旧启动器运行时中的两张 PNG 与许可文本转换为声明式 `.codexskin`。
- 转换结果固定标记为不可公开再分发、不可商用，不会进入公开 Release 或内置皮肤目录。
- 转换器不会打包 `.command`、CSS、JavaScript、二进制或任何可执行内容。
- 新增模板允许列表、data URL 嵌入、确定性私有包和主动内容隔离测试。
- 修复柳七月管理器主图只有凰焰背景、没有人物的问题；转换器现在把背景与透明人物层确定性合成为 16:10 `preview.png`，并将修正版私有包升级到 `1.0.1`。

## 1.0.1 — 2026-07-17

### 公开发行

- 两套皮肤版本提升到 `1.0.1`，允许在保留素材声明的前提下随皮肤包进行非商用公开分发。
- 新增基于 `SkinPackageExporter` 的确定性 Release 构建器，可生成两套 `.codexskin`、macOS 应用压缩包和 `SHA256SUMS.txt`。
- 管理器版本提升到 `1.0.1`（build 2），并增加公开发行包的自动化契约测试。

### 修复

- 将玄刃夜行从旧启动器选择器迁移到通用管理器契约：`codex-skin-template-nightblade-v1` 与 `codex-skin-manager-chrome`。
- 修复恢复默认后，同一 Codex 渲染进程因残留禁用标记而无法再次应用皮肤的问题。
- 一次性注入现在会在返回成功前强制执行运行时校验；模板根选择器未命中时不再误报成功。
- 为两套内置模板增加唯一运行时标记，并加入红莲 → 玄刃 → 恢复 → 再应用的真实 CDP 回归测试。

### 体验

- 启动时不再把 `active.json` 的持久记录冒充为当前已生效状态。
- 状态区分“上次使用记录”和“皮肤已验证生效”。
- 当前皮肤允许“重新应用”，失败后主按钮变为“重试切换”。
- 错误状态可直接打开注入日志。
- 皮肤卡片区分“上次使用”和“已验证生效”。
- 主要按钮改为描述结果的“切换到此皮肤 / 重新应用 / 重试切换”。

### 文档与交接

- 新增产品原则、架构边界、独立构建说明和跨设备 AI 上下文入口。
- 新增公开下载、首次打开、素材边界和 Release 校验说明。
