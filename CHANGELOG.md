# Changelog

## 1.1.0 — 未发布

### 本机私有皮肤迁移

- 新增管理器签名模板 `undying-phoenix-v1`，用于“柳七月 · 不死凰焰”。
- 新增 `build_private_liu_qiyue_skin.py`，可把旧启动器运行时中的两张 PNG 与许可文本转换为声明式 `.codexskin`。
- 转换结果固定标记为不可公开再分发、不可商用，不会进入公开 Release 或内置皮肤目录。
- 转换器不会打包 `.command`、CSS、JavaScript、二进制或任何可执行内容。
- 新增模板允许列表、data URL 嵌入、确定性私有包和主动内容隔离测试。

## 1.0.1 — 2026-07-17

### 公开发行

- 两套皮肤版本提升到 `1.0.1`，允许在保留素材声明的前提下随皮肤包进行非商用公开分发。
- 新增基于 `SkinPackageExporter` 的确定性 Release 构建器，可生成两套 `.codexskin`、macOS 应用压缩包和 `SHA256SUMS.txt`。
- 管理器版本提升到 `1.0.1`（build 2），并增加公开发行包的自动化契约测试。

### 修复

- 将玄刃夜行从旧启动器选择器迁移到通用管理器契约：`codex-skin-template-nightblade-v1` 与 `codex-skin-manager-chrome`。
- 修复恢复默认后，同一 Codex 渲染进程因残留禁用标记而无法再次应用皮肤的问题。
- 一次性注入现在会在返回成功前强制执行运行时校验；模板根选择器未命中时不再误报成功。
- 为两套内置模板增加唯一运行时标记，并加入红莲 → 玄刃 → 恢复 → 再应用的真实 CDP 回归测试。

### 体验

- 启动时不再把 `active.json` 的持久记录冒充为当前已生效状态。
- 状态区分“上次使用记录”和“皮肤已验证生效”。
- 当前皮肤允许“重新应用”，失败后主按钮变为“重试切换”。
- 错误状态可直接打开注入日志。
- 皮肤卡片区分“上次使用”和“已验证生效”。
- 主要按钮改为描述结果的“切换到此皮肤 / 重新应用 / 重试切换”。

### 文档与交接

- 新增产品原则、架构边界、独立构建说明和跨设备 AI 上下文入口。
- 新增公开下载、首次打开、素材边界和 Release 校验说明。
