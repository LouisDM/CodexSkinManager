# AI 上下文

本项目使用 `.ai-context/` 在不同 AI 工具之间共享上下文。

- `个人偏好.md`：长期稳定的个人偏好。
- `当前进度.md`：当前目标、完成情况和下一步。
- `长期知识.md`：确认过的决策和可复用经验。

## 快捷口令

- `初始化上下文`：创建缺失的上下文文件，不覆盖已有内容。
- `加载上下文`：读取三个文件，简述当前目标、约束和下一步。
- `保存进度`：更新当前进度；删除旧内容或写入长期内容前先征得用户确认。
- `整理上下文`：检查重复、冲突和过期内容，修改前先征得用户确认。

用户当前明确指令优先。不得保存密码、令牌、密钥或不必要的敏感信息。

## Worktree 约定

- 项目内临时 worktree 统一放在 `.worktrees/<branch-or-task-name>/`。
- `.worktrees/` 必须保持在 `.gitignore` 中，避免误提交嵌套工作区。
- 创建 worktree 前先确认当前工作树是否有未提交改动；不要切换或清理用户正在使用的分支。
- 若已有项目内 `.worktrees/`，优先使用它；没有时也按该目录创建，不再退到全局 worktree 目录。
