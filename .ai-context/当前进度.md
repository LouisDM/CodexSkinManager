# 当前进度

## 目标

- 维护并发布原生 macOS `Codex Skin Manager`，让 `.codexskin` 的导入、切换、恢复和故障自救可信、可测试、可跨设备继续开发。
- 当前交付目标：将仓库转为公开，发布管理器和两套可直接下载导入的非商用同人皮肤。
- 当前开发目标：在不公开受限素材的前提下，将“柳七月 · 不死凰焰”旧 `.command` 启动器安全迁移到管理器 1.1.0。

## 已完成

- 2026-07-17：定位玄刃夜行“管理器提示成功但皮肤未切换”的根因。旧模板仍使用 `codex-meng-chuan-nightblade-*`，而通用引擎创建的是 `codex-skin-template-nightblade-v1` 和 `codex-skin-manager-chrome`。
- 已迁移玄刃夜行模板选择器，并补充红莲 → 玄刃 → 恢复 → 再应用的真实 Chrome CDP 回归测试。
- 一次性注入会在返回成功前验证模板运行时标记，避免 CSS 根选择器未命中时误报成功。
- 修复恢复默认后的渲染进程残留禁用标记；只有显式一次性应用可清除该标记，watch 修复流程不能清除，避免恢复竞争。
- UI 已区分“上次使用记录”和“已验证生效”；支持“重新应用”“重试切换”和错误区直接“查看日志”。
- 已在真实 Codex 上验证红莲业火与玄刃夜行均可切换；最终保留玄刃夜行，运行时标记为 `nightblade-v1`，蓝色 shimmer 为 `rgb(145 220 255 / 0.95)`。
- 已写入 `CHANGELOG.md`、`README.md`、`docs/skin-manager/PRODUCT_PRINCIPLES.md` 和 `docs/skin-manager/ARCHITECTURE.md`。
- 已从独立目录通过 60 个 Swift 测试、5 个真实浏览器/CDP 测试、内置资源校验、已安装应用包校验、隔离启动验收和独立 release 应用包构建。
- 已创建独立 Git 仓库，初始源码提交为 `ad3c348`。
- 已创建并推送私有仓库：`https://github.com/LouisDM/CodexSkinManager`，默认分支为 `main`。
- 已建立公开发行契约：皮肤升级为 `1.0.1`，只允许保留声明的非商用同人分发；管理器升级为 `1.0.1`（build 2）。
- 已新增基于核心 `SkinPackageExporter` 的 Release 构建器和确定性发行包测试。
- 已通过 PR `#1` 将公开发行变更合并到 `main`，合并提交为 `0d03519`。
- GitHub 仓库已改为公开：`https://github.com/LouisDM/CodexSkinManager`。
- 正式 Release 已发布：`https://github.com/LouisDM/CodexSkinManager/releases/tag/v1.0.1`。
- Release 包含管理器 ZIP、玄刃夜行 `.codexskin`、红莲业火 `.codexskin` 和 `SHA256SUMS.txt`；已从 GitHub 回下载并通过 SHA-256 校验。
- 新版本完整验证包括 60 个 Swift 测试、5 个真实浏览器/CDP 测试、公开包确定性测试、资源校验、应用签名/版本校验和隔离启动验收。
- 2026-07-17：完成“柳七月 · 不死凰焰”旧启动器迁移。新增管理器签名模板 `undying-phoenix-v1` 和本机私有转换器 `scripts/build_private_liu_qiyue_skin.py`。
- 转换器只打包 `hero-background.png`、`hero-character.png`、主题/权利 JSON 与许可文本；旧 `.command`、CSS、JavaScript 和二进制不会进入 `.codexskin`。
- 柳七月素材许可仅允许私用预览与评估；图片和生成包均不进入公开仓库、内置皮肤或 GitHub Release，包内 `redistributionAllowed` 与 `commercialUse` 均为 `false`。
- 本机私有包位于 `~/Applications/Liu-Qiyue-Undying-Phoenix-1.0.0.codexskin`，已通过管理器真实导入并安装到 `~/Library/Application Support/CodexSkinManager/skins/liu-qiyue-undying-phoenix/1.0.0`。
- 管理器已升级并安装为 `1.1.0`（build 3）；柳七月皮肤已在官方 Codex 的 `127.0.0.1:9340` 会话中切换成功，运行时 `--verify` 在 2 个目标中通过，watcher 正常守护。
- 本轮验证通过 61 个 Swift 测试、5 个真实浏览器/CDP 测试、2 个私有包转换测试、资源校验、公开 Release 确定性测试、已安装应用包校验和隔离端到端启动测试。
- 2026-07-17：新增通用声明式制作入口 `scripts/build_codexskin.py`。新皮肤直接以 `skin.json + assets + LICENSES` 为唯一源数据，生成确定性 data-only `.codexskin`，不再先制作 `.command`、独立端口或注入运行时。
- 打包器会严格校验模板、令牌、相对路径、PNG/JPEG、许可和权利声明，自动生成 manifest 与 SHA-256，并确保 CSS、JavaScript、Shell 和其他主动内容不会进入包。
- 新增 `tests/test_skin_authoring_packager.py` 和 Swift `SkinAuthoringPackagerCompatibilityTests`；后者会把 Python 生成的真实包交给 `SkinPackageImporter`，防止作者工具与管理器合约漂移。
- 新增 `docs/skin-manager/AUTHORING.md`；CodexUI 的 `docs/codex-cdp-skin-launcher.md` 已改为“直接制作 `.codexskin`”主流程，旧 `.command` 仅保留为迁移附录。
- 本轮最终验证通过 62 个 Swift 测试、5 个真实 Chrome/CDP 测试、2 个通用打包测试、2 个私有迁移测试、内置资源与公开 Release 确定性校验、已安装应用包校验、隔离端到端启动和官方 Codex 深度签名检查。
- 2026-07-17：作者文档补充 Git 交付规则。用户要求提交、push、PR 或 Release 时，默认同时上传对应皮肤源文件和宽窄屏截图；权利允许时上传 `.codexskin` 与 SHA-256，受限或权利不明素材不得进入公开 Git/Release，必须列出排除项并改走明确授权的私有渠道。
- 2026-07-17：仓库入口改为产品化三步路径“先给 3 个方案 → 用户选 A/B/C → 制作并按需提交”。CodexUI 主指南和两个 README 均提供可复制的柳七月提示词、预期交付物与 Git/权利边界，第一次使用不再要求用户理解模板、端口或 JSON。
- 2026-07-17：新增 CodexUI `$convert-to-codexskin` Skill 与只读旧皮肤检查器，支持目录、单文件和 ZIP 清点；README/作者规范加入 `.command`、CSS+图片、ZIP 和旧 JSON 的转换提示词。旧脚本绝不执行，CSS 只映射到管理器模板，权利不明时不得公开分发。
- 2026-07-17：确认资料库出现 5 张卡片的根因是孟川两套皮肤的 `1.0.0` 与 `1.0.1` 同时安装，而 UI 直接展示了全部版本目录。现已按皮肤 ID 只展示最高语义版本，5 个安装版本会呈现为 3 套皮肤；旧版本继续保留，最近使用状态按皮肤身份关联。README 明确当前为两套公开皮肤加一套柳七月本机私用皮肤。
- 本轮验证通过 64 个 Swift 测试、5 个真实 Chrome/CDP 测试、资源/作者工具/私有迁移/Release 契约、已安装应用包检查和隔离端到端启动；重建本机应用后通过辅助功能树确认资料库只有柳七月、红莲业火和玄刃夜行 3 张卡片，孟川两套均展示 `1.0.1`。
- 2026-07-17：修复“柳七月 · 不死凰焰”详情显示不全。根因不是素材损坏，而是旧三栏和原生 Inspector 在窗口压缩后仍按理想宽度测量；柳七月更宽的预览图、较长路径和权利说明最先触发水平裁切。
- 详情区已改为资料库与详情共用的原生 `HSplitView`：分隔线可拖动调整宽度，工具栏或 `⌥⌘I` 可独立收起/展开；左侧系统导航栏仍可单独隐藏。
- 详情内容不再手算 Inspector 宽度。预览使用不参与尺寸测量的弹性 16:10 容器，安全/权利卡片使用自适应单列或双列，操作区通过 `ViewThatFits` 自动切换横排/纵排，长路径限制在元数据行内。
- 已重建并安装到 `~/Applications/Codex 皮肤管理器.app`；真实界面逐项验证柳七月完整标题、预览、双状态卡、权利提示和状态文案，验证详情收起后资料库占满窗口、再次展开正常，并复核红莲业火与玄刃夜行详情均完整。
- 本轮最终验证通过 66 个 Swift 测试、5 个真实 Chrome/CDP 测试、资源/作者工具/私有迁移/Release 契约、已安装应用包检查和隔离端到端启动；`git diff --check` 通过。
- 2026-07-17：继续优化资料库交互。确认旧布局即使已修复裁切，仍存在“低频筛选独占左栏、中央大卡片与右侧大预览重复、三套皮肤却出现三层导航”的产品问题。
- 主界面现已改为真正的双栏主从工作区：详情展开时，左侧是可直接切换皮肤的紧凑视觉导航，显示缩略图、名称、作者、版本、授权与活动状态；右侧是唯一的完整预览、安全信息和应用操作区。
- “全部皮肤、最近使用、仅限本机、未验证来源”四种筛选保留并收进列表顶部菜单，同时显示可见数量与总数。详情收起时资料库自动切换为响应式画廊，重新展开后恢复紧凑导航。
- 已重建安装到 `~/Applications/Codex 皮肤管理器.app` 并通过真实辅助功能树验证：三套皮肤均可直接选择，红莲业火与玄刃夜行会同步更新右侧详情，筛选菜单四项完整，详情收起/画廊/重新展开流程正常；柳七月私有图片及其包仍未进入 Git。
- 本轮最终验证通过 68 个 Swift 测试、5 个真实 Chrome/CDP 测试、资源/作者工具/私有迁移/Release 契约、已安装应用包检查、隔离端到端启动和 `git diff --check`。
- 2026-07-17：修复柳七月在管理器中的主图与实际皮肤不一致。根因是私有转换器把 `manifest.preview` 直接指向不含人物的 `assets/background.png`，而实际模板会另外叠加 `assets/hero.png`；App 按契约正确显示了错误声明的纯背景。
- `build_private_liu_qiyue_skin.py` 现使用 Pillow 将背景与透明人物层确定性合成为 1600×1000 `preview.png`，清单改指向该最终主图，并将私有包升级为 `1.0.1`，避免与已安装的不同内容 `1.0.0` 发生版本冲突。
- 新私有包位于 `~/Applications/Liu-Qiyue-Undying-Phoenix-1.0.1.codexskin`，已真实导入到 `~/Library/Application Support/CodexSkinManager/skins/liu-qiyue-undying-phoenix/1.0.1`。双栏大图、左侧缩略图和收起后的画廊卡片均已确认显示人物；受限原图、合成预览和 `.codexskin` 仍未进入 Git。
- 修复后完整 `npm test` 通过：68 个 Swift 测试、5 个真实 Chrome/CDP 测试，以及内置资源、作者打包器、柳七月私有迁移和公开 Release 契约测试全部通过。
- 2026-07-17：将 `.codexskin` 导入与导出统一到窗口顶部“皮肤文件”操作组，并在“文件”菜单提供 `⌘O` 导入和 `⇧⌘E` 导出；移除资料库左下角孤立导入和详情“更多”中的隐藏导出。柳七月等私有皮肤仍显示锁定和授权原因，红莲/玄刃可直接导出。
- 修复导出包回导同一资料库的假版本冲突：图片在首次导入时会被净化，因此净化后导出的 manifest 描述符与原始包不同；资料库现在比较稳定元数据和实际净化后文件内容，内容真正不同的同版本包仍拒绝。
- 已重建安装到 `~/Applications/Codex 皮肤管理器.app`，通过真实界面导出红莲为 `/tmp/CodexSkinManager-UI-Export-26327.codexskin`；该文件与 `dist/v1.1.0/Meng-Chuan-Red-Lotus-1.0.1.codexskin` SHA-256 一致，并已从顶部导入入口无错误回导，资料库仍为 3 套皮肤。
- 本轮最终验证通过 72 个 Swift 测试、5 个真实 Chrome/CDP 测试、资源/作者工具/私有迁移/Release 契约、已安装 App 包校验、隔离端到端启动和真实 App 导出回导闭环。
- 2026-07-17：修复三套皮肤的侧栏主题名与左下角用户名重叠。根因是主题名使用 `nav.sidebar-foreground-muted::after`，作为默认顺序的最后一个 Flex 项占用侧栏底部 33px，而 Codex 原生账户栏以绝对定位固定占用同一底部 46px。
- 玄刃夜行、红莲业火和不死凰焰统一把装饰标题、主题名和原生侧栏内容的顺序设为 `-2 / -1 / 0`；主题名现位于左上身份区的装饰标题下方，不占用用户名、任务列表或主内容区。
- 新增三模板真实浏览器布局契约测试。新版已安装到 `~/Applications/Codex 皮肤管理器.app`，逐套在真实 Codex 中切换并测量：主题名 `y=105–120`，用户名 `y=1045.5–1074.5`，三套交叠量均为 `0px`；最终恢复“柳七月 · 不死凰焰”为已验证生效皮肤。
- 本轮最终验证通过 72 个 Swift 测试、6 个真实 Chrome/CDP 测试、资源/作者工具/私有迁移/Release 契约、已安装 App 包校验、隔离端到端启动和 `git diff --check`。
- 2026-07-17：为验证新皮肤导入和使用流程，新增三套声明式验证皮肤源目录：`hakimi-paw-atelier`、`cai-xukun-stage-check`、`tifa-seventh-heaven-flow`。
- 三套素材均由 `scripts/generate_validation_skin_assets.py` 确定性生成原创 PNG。哈基米为可公开非商用测试包；蔡徐坤与蒂法为本机私用 fan-made 概念，不含真人照片、官方角色图、Logo、引语、CSS、JS 或可执行内容，`redistributionAllowed=false`。
- 已生成本地包：`dist/validation-skins/Hakimi-Paw-Atelier-1.0.0.codexskin`、`dist/validation-skins/Cai-Xukun-Stage-Check-1.0.0.codexskin`、`dist/validation-skins/Tifa-Seventh-Heaven-Flow-1.0.0.codexskin`；`dist/` 仍为本机产物不进 Git。
- 已通过真实「Codex 皮肤管理器」导入三套包，安装目录分别位于 `~/Library/Application Support/CodexSkinManager/skins/<id>/1.0.0`。
- 新增 Swift 兼容测试 `testRepositoryValidationSkinsBuildImportAndRespectExportRights`，覆盖三套源皮肤从 Python 打包器到 Swift 导入器、临时资料库安装、哈基米可导出、蔡徐坤/蒂法导出锁定。
- 验证通过：`python3 tests/test_skin_authoring_packager.py`、`cd skin-manager && swift test --filter SkinAuthoringPackagerCompatibilityTests`、`npm run test:engine`，并用三套已安装目录逐个在 headless CDP fixture 中验证运行时 `skinId` 与模板标记匹配。
- 2026-07-17：用户指出三套验证皮肤视觉粗糙，要求先搜索再选择最推荐方向制作。已对比孟川成品与三套验证皮肤，确认上一版问题是批量流程优先导致视觉层缺少空间、光影、角色质感和真实皮肤预览感。
- 搜索后选择优先重制 `hakimi-paw-atelier`：哈基米在中文互联网中已衍生为猫或可爱事物语义，授权风险最低，适合作为可公开非商用验证皮肤；蔡徐坤与蒂法仍应仅作为私用 fan-made，不使用真人照片或官方 IP 图。
- `hakimi-paw-atelier` 已升级为 `1.0.2`，重制为高质感猫咪陪伴工作台，并新增独立 `paw-atelier-v1` 模板，避免复用 `nightblade-v1` 时混入孟川/玄刃夜行文案。
- 新包位于 `dist/validation-skins/Hakimi-Paw-Atelier-1.0.2.codexskin`，SHA-256 为 `2c6b35f196dd035cbf112f43fe1dc34270b7c2d8f0462f2e75844003084f907b`。已通过真实管理器导入到 `~/Library/Application Support/CodexSkinManager/skins/hakimi-paw-atelier/1.0.2`，并在 headless CDP fixture 中验证应用、校验和恢复流程：`skinId=hakimi-paw-atelier`，模板标记 `paw-atelier-v1`。
- 2026-07-17：按用户反馈“需要人物”重制另外两套私用验证皮肤。`cai-xukun-stage-check` 升级为 `1.0.1`，使用人物舞台肖像和独立 `stage-check-v1` 模板，新包 SHA-256 为 `83c0d659918c27dfe9ad0442cd8af17b554c8ae0a98ab8f351160a27108ae914`。`tifa-seventh-heaven-flow` 升级为 `1.0.1`，使用同人格斗角色立绘和独立 `seventh-heaven-v1` 模板，新包 SHA-256 为 `08fb3c39eb76b1188d389522e0b9d3ca588f935043083c0c4e0b9447ac4d2431`。两套均已导入真实管理器并通过 headless CDP 应用、校验和恢复流程；仍保持 `redistributionAllowed=false`。

## 待处理

- 当前公开发行交付没有阻塞项。
- 柳七月素材当前禁止公开再分发；除非获得并记录新的明确授权，不得把两张 PNG、私有 `.codexskin` 或该皮肤加入公开 Release。
- 1.1.0 源码改动可公开：只包含通用模板、转换器、测试和文档，不包含受限图片；尚未创建 1.1.0 Release。
- 通用打包器只支持管理器当前允许列表中的模板；新增模板时必须同步更新 Swift、Node `TEMPLATE_CATALOG`、打包器和相关测试。
- 可选后续：使用 Apple Developer ID 签名并公证应用，消除首次打开时的 Gatekeeper 右键操作。
- 可选后续：实现“选择即临时预览、Enter 确认、取消自动恢复”的预览模式；必须保持预览与已验证生效状态严格分离。

## 下一步

- 新设备克隆公开仓库后先执行 `加载上下文`。
- 开发前运行：`npm install && npm test`。
- 重建发行资产：`npm run build:release`。
- 下一版本发布前必须更新版本、Release 说明和 `SHA256SUMS.txt`，并从 GitHub 回下载复核。
- 跨设备使用柳七月皮肤时，需通过私人渠道单独传输 `.codexskin`；克隆公开仓库只能获得支持该包的管理器代码，不会获得受限素材。
